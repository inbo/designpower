% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_power.R
\name{find_power}
\alias{find_power}
\title{Find Optimal Design}
\usage{
find_power(
  design,
  design_digits,
  opti,
  sim_power,
  extra_args = list(),
  power = 0.9,
  alpha = 0.1,
  filename = "power.duckdb"
)
}
\arguments{
\item{design}{List. Initial design parameters.}

\item{design_digits}{Named numeric. Precision (number of decimal places) for
each parameter.
The names must match those in \code{design}.}

\item{opti}{Character. Name of parameter to optimize.}

\item{sim_power}{Function. Simulation function (should accept \code{design} and
\code{n_sim}).}

\item{extra_args}{List. Optional additional arguments passed to \code{sim_power}.
Defaults to an empty list.}

\item{power}{Numeric. Target power (default 0.9).}

\item{alpha}{Numeric. Significance level (default 0.1).}

\item{filename}{Character. Path to \code{DuckDB} database file.}
}
\value{
Numeric vector. The optimized parameter value and confidence range.
}
\description{
Iteratively search for optimal design parameters using adaptive simulation.
}
\details{
Uses \code{DuckDB} to store simulations and gradually refines design parameters
to achieve target power.
}
\examples{
\dontrun{
find_power(
  design = list(trend = -0.03, n_site = 20, n_year = 12),
  design_digits = c(trend = 4, n_site = 0, n_year = 0),
  opti = "trend",
  sim_power = sim_power,
  extra_args = list()
)
}

}
